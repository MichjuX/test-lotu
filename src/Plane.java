import java.awt.*;

public class Plane extends AirShip {
    public Plane(Point startPos, int height, int width, int altitude) {
        super(startPos, Color.green, height, width, altitude);
    }
}
